# 全量依赖（服务端 + 客户端）
#
# 推荐按角色安装：
#   仅服务端: pip install -e ".[server]"
#   仅客户端: pip install -e ".[client]"
#   全部功能: pip install -e ".[server,client]"
#
# 兼容旧习惯时，也可以直接使用：
#   pip install -r requirements.txt

websockets>=11.0
psutil>=5.8
pillow>=8.0
cryptography>=41.0
pyinstaller>=5.0

# 服务端
flask>=2.0
flask-sock>=0.7.0
pystray>=0.19

# 客户端
pyautogui>=0.9.53
pynput>=1.7
pywinpty>=0.15; platform_system == "Windows"
opencv-python>=4.5
numpy>=1.20
